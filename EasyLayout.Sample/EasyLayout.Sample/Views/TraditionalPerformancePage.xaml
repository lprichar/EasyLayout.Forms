<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:EasyLayout.Sample.Controls"
             x:Class="EasyLayout.Sample.Views.TraditionalPerformancePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackLayout Grid.Row="0" HorizontalOptions="Center">
            <controls:PerfLabel x:Name="MainTitle" Text="Title Section" Font="Bold,Medium" />
            <controls:TraditionalHeader x:Name="TraditionalHeader"></controls:TraditionalHeader>
            <Button Clicked="Button_OnClicked" Text="Get Stats()"></Button>
        </StackLayout>

        <ScrollView Grid.Row="1">
            <controls:PerfLabel x:Name="Description" Text="Lots and lots of text goes here" Font="Small" />
        </ScrollView>
    </Grid>
    
</ContentPage>